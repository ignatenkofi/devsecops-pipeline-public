# Байткод: появляется от любого локального импорта pins.py и от прогона
# линтов. Две такие копии уже пролежали закоммиченными в публичном репо —
# у приватного близнеца это правило было, у публичного нет.
__pycache__/
*.py[cod]

sarif/
*.sarif
.DS_Store
* 2.*
